{
  "name": "@paymanai/genie-mcp-stdio",
  "version": "0.2.0",
  "description": "Genie for any stdio-only MCP host: a local stdio bridge to the remote Genie MCP server",
  "license": "MIT",
  "author": "Payman",
  "homepage": "https://github.com/PaymanAI/genie-mcp-stdio#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PaymanAI/genie-mcp-stdio.git"
  },
  "bugs": {
    "url": "https://github.com/PaymanAI/genie-mcp-stdio/issues"
  },
  "type": "module",
  "bin": {
    "genie-mcp-stdio": "dist/src/index.js"
  },
  "files": [
    "dist/src",
    "README.md",
    "docs"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test \"dist/test/**/*.test.js\"",
    "prepublishOnly": "npm test"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "genie",
    "payman",
    "stdio",
    "payments"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0"
  },
  "devDependencies": {
    "@types/node": "24.5.2",
    "typescript": "5.9.2"
  }
}
